# Mowito_rep

* The trained models are saved in models folder
* To test the models with images follow this
    1. clone the repository.
    2. go into code implementation directory.
    3. run the codes by changing the **path to image** which is present at the end of script.
